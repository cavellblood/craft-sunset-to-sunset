{% macro configWarning(setting) -%}
    {% set setting = '<code>'~setting~'</code>' %}
    {{ "This is being overridden by the {setting} config setting in your <code>{file}</code> config file."|t('sunset-to-sunset', {
        setting: setting,
        file: 'sunset-to-sunset.php'
    })|raw }}
{%- endmacro %}